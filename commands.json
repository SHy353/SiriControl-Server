{
  "commands": {
    "lights": {
      "required": ["turn", "lights"],
      "options": ["on", "off"]
    },
    "volume": {
      "required": ["turn", "volume"],
      "options": ["up", "down"]
    }
  }
}
